# File: fibonacci-slow.fg
# Date: Oct  5, 2010
# Mod.: Dec 18, 2010  -- removed the complicated parts.
#


fib(n) =>
    if( n < 2
        n
        fib(n - 1) + fib(n - 2) )

START = 0
END   = 20

loop(n) =>
    if( n > END
        void
        do( print(fib(n))
            loop(n+1) ) )

loop(START)

