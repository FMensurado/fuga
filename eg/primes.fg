

isprime(n) {
  result = true
  i = 2
  while(i < n, do(
    if ([n % i] == 0, result := false)
    i := [i + 1]
  ))
  result
}

primes_upto(n) {
  result = ()
  range(2,n) map(i, if(isprime(i), do(result append!(i), true), false))
  result
}

print(primes_upto(60))

